/*
 *SWGR / Assigned Feeder Editor 
 */

	//프로젝트 정보
	//parent.getProjectInfo();
	
	var getProjectInfo = function (jsonObj) {
		//기본적인 SESSION정보 포함
	    alert("getProjectInfo : "+jsonObj);
	};
	
	//스위치 타입 셀렉트 박스
	//parent.getSwitchgearTypeList();
	
	var getSwitchgearTypeList = function (jsonList) {
	    alert("getSwitchgearTypeList : "+jsonList);
	};
	
	//스위치기어 목록(미사용)
	//parent.getSwitchgearUnused();
	
	var getSwitchgearUnused = function (jsonList) {
	  alert("getSwitchgearUnused : "+jsonList);
	};
	
	//스위치기어 목록(사용)
	//parent.getSwitchgearUse();
	
	var getSwitchgearUse = function (jsonList) {
	  alert("getSwitchgearUse : "+jsonList);
	};
	
	//load 목록(미사용)
	//parent.getLoadUnused();
	
	var getLoadUnused = function (jsonList) {
	  alert("getLoadUnused : "+jsonList);
	};
	
	//load 목록(사용)
	//parent.getLoadUse();
	
	var getLoadUse = function (jsonList) {
	  alert("getLoadUse : "+jsonList);
	};
	
	//feeder 목록
	//parent.getFeederList();
	
	var getFeederList = function (jsonList) {
	  alert("getFeederList : "+jsonList);
	};

	// 트리에서 선택한 노드를 지운다.
	var deleteFeeder = function (seq) {

	};
/*
 *Hierarchy Editor 
 */
	
/*
 *BLDG / Route Editor 
 */